.venv/
__pycache__/
*.pyc
.env
# .env matches that name only. An editor swap file is called .env.swp, which
# slipped past it once and published a live key — so ignore the whole family.
.env.*
!.env.example
*.swp
*.swo
*~
.pytest_cache/

# OSM extracts and the built POI database — large, and rebuildable.
data/
