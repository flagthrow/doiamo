fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
# Reading a sentence into a search. Optional at runtime — without a key the
# rules still answer — but it must be installed or the fallback never exists.
anthropic>=0.69
